// Copy this file to config.js and fill in your key. config.js is gitignored —
// never commit a real key. Without a config.js, Define falls back to the free
// dictionary + Wikipedia lookups.
//
// Get a free-tier key at https://aistudio.google.com/apikey
const RM_CONFIG = {
  GEMINI_API_KEY: "",
};
